@model GCTL.Core.ViewModels.AttendanceManagement.ScheduleManagement.OfficeDayRoster.RosterInOfficeDaysSetupVM

@{
    var employees = ViewBag.EmployeeList as List<GCTL.Core.ViewModels.AttendanceManagement.ScheduleManagement.OfficeDayRoster.RosterInOfficeDaysSetupVM>;
}

<style>
    element.style {
        top: 276.25px;
        left: 1378.03px;
        user-select: none;
    }

    .date-picker-wrapper {
        margin-left: 1.2vw !important;
    }
</style>

<form asp-controller="OfficeDayRoster" asp-action="Create" id="rosterInOfficeDays-form" enctype="multipart/form-data">
    <div class="row g-3 mb-3">
        <div class="col-md">
            <label class="form-label" asp-for="OrganizationID">Organization Name</label>
            <select class="form-select" asp-for="OrganizationID" asp-items="@ViewBag.OrganizationDD as SelectList">
                <option value="">Select Organization...</option>
            </select>
        </div>

        <div class="col-md department">
            <label class="form-label" asp-for="DepartmentIDs">Department Name</label>
            <select class="form-multi-select"
                    asp-for="DepartmentIDs"
                    asp-items="@ViewBag.DepartmentDD as SelectList"
                    multiple data-coreui-multiple="true"
                    data-coreui-selection-type="counter"
                    data-coreui-search="true"
                    data-coreui-placeholder="Select Department...">
            </select>
        </div>

        <div class="col-md employee">
            <label class="form-label" asp-for="EmployeeIDs">Employee Name</label>
            <select class="form-multi-select"
                    asp-for="EmployeeIDs"
                    multiple data-coreui-multiple="true"
                    data-coreui-selection-type="counter"
                    data-coreui-search="true"
                    data-coreui-placeholder="Select Employee...">
                @foreach (var group in employees.GroupBy(e => e.DepartmentName))
                {
                    <optgroup label="@group.Key">
                        @foreach (var emp in group)
                        {
                            <option value="@emp.EmployeeID">@emp.EmployeeName</option>
                        }
                    </optgroup>
                }
            </select>
        </div>

        <div class="col-md">
            <label class="form-label" asp-for="ShiftID">Shift Name</label>
            <select class="form-select" asp-items="@ViewBag.ShiftDD as SelectList" asp-for="ShiftID">
                <option value="">Select Shift...</option>
            </select>
        </div>

        <div class="col-md">
            <label class="form-label" for="timepicker2">Select Date Range</label>
            @* <input class="form-control datetimepicker" id="timepicker2" type="text" placeholder="DD/MM/YYYY to DD/MM/YYYY" data-options='{"mode":"range","dateFormat":"d/m/y","disableMobile":true}' /> *@
            <input type="text" id="basic-daterange" class="form-control date-input" placeholder="d/m/y to d/m/y" readonly>
            <input type="hidden" id="basic-daterange_fromHidden" name="FromDate" />
            <input type="hidden" id="basic-daterange_toHidden" name="ToDate" />
        </div>

    </div>
    <div class="col-12">
        <div class="row g-3 justify-content-end">
            <div class="col-auto">
                <button type="reset" class="btn btn-phoenix-primary px-5" id="rosterInOfficeDays-resetBtn">@Html.SmartLocalize("Reset")</button>
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-primary px-5 px-sm-10" id="rosterInOfficeDays-saveBtn">@Html.SmartLocalize("Publish")</button>
            </div>
        </div>
    </div>
</form>
@* data-coreui-cleaner="true|false" — Show or hide the “clear selection” button
data-coreui-disabled="true|false" — Enable or disable the dropdown
data-coreui-invalid="true|false" — Apply invalid state styling
data-coreui-multiple="true|false" — Allow single or multi-select
data-coreui-name="string" — Set form name attribute
data-coreui-options-max-height="[value]" — Max‑height of dropdown (e.g., 200px)
data-coreui-options-style="checkbox|text" — Display checkboxes or plain text lists
data-coreui-placeholder="string" — Input placeholder, e.g. "Select..."
data-coreui-search="true|false" — Enable search/filter input
data-coreui-search-no-results-label="string" — Customize “no results” message
data-coreui-select-all="true|false" — Show or hide a “Select All” option
data-coreui-select-all-label="string" — Set custom label for “Select All”
data-coreui-selection-type="tag|counter|text" — Display selected items as tags, counter, or comma‑separated text
data-coreui-selection-type-counter-text="string" — Change counter text, e.g. "items selected"
data-coreui-valid="true|false" — Apply valid state styling *@