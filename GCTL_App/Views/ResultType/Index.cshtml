@model GCTL_App.ViewModels.MasterSetup.ResultType.ResultTypePageVM

@{
    ViewData["Title"] = @Html.SmartLocalize("Result Type");
}

<div class="row g-2">
    <div class="col-12 col-md-7 col-xl-5 col-xxl-5">
        <partial name="_Form" model="Model.Setup" />
    </div>

    <div class="col-12 col-md-7 col-xl-7 col-xxl-7">
        <partial name="_Table" />
    </div>
</div>

<partial name="_ConfirmDelete" />

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
	<script src="~/js/pages/mastersetup/resulttype.js"></script>
    <script type="text/javascript">
        $(function () {
            var options = {
                baseUrl: '@Url.Action("", "ResultType")'
            };
            options.load = () => {

            };

            $.resulttype(options);
        });
    </script>
}
