@model GCTL.Core.ViewModels.MasterSetup.LicenceType.LicenceTypeVM

<div class="card h-100">
    <div class="card-header border-bottom-1">
        <div class="row justify-content-between align-items-center">
            <div class="col-auto">
                <h3 class="text-body-emphasis mt-3">@Html.SmartLocalize("Add Licence Type")</h3>
            </div>
        </div>
    </div>

    <div class="card-body mt-2">
        <form asp-action="Create" asp-controller="LicenceType" method="post" enctype="multipart/form-data" id="licenceType-form">
            @Html.AntiForgeryToken()
            <div class="col-sm-12 col-md-12 mb-2">
                <input type="hidden" asp-for="LicenceTypeID" />
                <div class="flatpickr-input-container">
                    <div class="form-floating">
                        <input class="form-control" asp-for="LicenceTypeName" type="text" placeholder="@Html.SmartLocalize("Licence Type Name")" />
                        <label asp-for="LicenceTypeName">@Html.SmartLocalize("Licence Type Name") <span style="color: red;">*</span></label>
                    </div>
                    <span asp-validation-for="LicenceTypeName" class="text-danger"></span>
                    <span id="nameError" class="text-danger" style="display:none;"></span>
                </div>
            </div>

            <div class="col-12 mb-2">
                <div class="row g-3 justify-content-end">
                    <div class="col-auto">
                        <button type="reset" class="btn btn-phoenix-primary px-5" id="licenceType-resetBtn">@Html.SmartLocalize("Reset")</button>
                    </div>
                    <div class="col-auto">
                        <button type="submit" class="btn btn-primary px-5 px-sm-15" id="licenceType-saveBtn">@Html.SmartLocalize("Save")</button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>
