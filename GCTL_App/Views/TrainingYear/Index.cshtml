@model GCTL_App.ViewModels.MasterSetup.TrainingYear.TrainingYearPageVM

@{
    ViewData["Title"] = @Html.SmartLocalize("Training Year");
}

<div class="row g-2">
    <div class="col-12 col-md-7 col-xl-5 col-xxl-5">
        <partial name="_Form" model="Model.Setup" />
    </div>

    <div class="col-12 col-md-7 col-xl-7 col-xxl-7">
        <partial name="_Table" />
    </div>
</div>

<partial name="_ConfirmDelete" />

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
	<script src="~/js/pages/mastersetup/trainingyear.js"></script>
    <script type="text/javascript">
        $(function () {
            var options = {
                baseUrl: '@Url.Action("", "TrainingYear")'
            };
            options.load = () => {

            };

            $.trainingyear(options);
        });
    </script>
}
