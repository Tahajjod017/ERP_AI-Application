(function ($) {
    $.offdayroster = function (options) {
        // Default options
        var settings = $.extend({
            baseUrl: '/',
        }, options);

        var gridUrl = settings.baseUrl + "/GetAll";
        $(() => {


        });

    }
}(jQuery));